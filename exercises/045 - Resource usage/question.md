# Question 45: Use built-in CLI tools to monitor resource usage

Use `kubectl` built-in commands to monitor cluster resource usage.

Perform these tasks:

- Show CPU and memory usage for all nodes.
- Show CPU and memory usage for Pods in all namespaces.
- List Pods in namespace `default` with node placement information.

Write the commands in `answer.md`.

