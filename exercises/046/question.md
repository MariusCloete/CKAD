# Question 46: Inspect Pod status and cluster events

Use `kubectl` built-in commands to investigate Pod status.

Perform these tasks:

- List all Pods in all namespaces.
- Describe a Pod named `web-0` in namespace `default`.
- Show recent cluster events sorted by creation time.

Write the commands in `answer.md`.

