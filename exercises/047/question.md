# Question 47: Retrieve logs from containers

Use `kubectl logs` to inspect application logs.

Perform these tasks:

- Show logs for Pod `app-pod`.
- Show logs for container `sidecar` in Pod `app-pod`.
- Show logs from the previous instance of container `app` in Pod `app-pod`.

Write the commands in `answer.md`.

