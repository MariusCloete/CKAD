# Question 37: Upgrade and rollback a Helm release

Assume release `web` in namespace `helm-lab` already exists.

Perform these tasks:

- Upgrade the release to set `replicaCount=3`.
- Verify rollout.
- Roll back to the previous revision.

Save your new values as `values-v2.yaml`.

