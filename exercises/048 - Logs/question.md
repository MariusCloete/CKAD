# Question 48: Stream and limit logs from a workload

Use `kubectl logs` to follow and limit log output.

Perform these tasks:

- Stream logs from Deployment `web`.
- Show only the last `20` log lines from Pod `web-abc123`.
- Show logs for all containers in Pod `multi-log-pod`.

Write the commands in `answer.md`.

