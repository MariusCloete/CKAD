# Question 49: Debug a Pod in CrashLoopBackOff

Run provided pod.yaml


Then investigate why it fails and fix it so the Pod stays running with command `sh -c "echo ok && sleep 3600"`.
Save the manifest as `pod.yaml`.

